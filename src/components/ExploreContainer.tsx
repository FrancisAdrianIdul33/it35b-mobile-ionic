import './ExploreContainer.css';

interface ContainerProps { }

const ExploreContainer: React.FC<ContainerProps> = () => {
  return (
    <div id="container">
      <strong>Lets build a Application</strong>
     
    </div>
  );
};

export default ExploreContainer;
